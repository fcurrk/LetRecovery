name: Build LetRecovery Windows

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: wy414012/toolchain@master
        with:
          profile: minimal
          toolchain: "stable" # stable
          override: true
      - name: build Desktop
        working-directory: desktop
        run: |
          cargo build --release --all-features --target=x86_64-pc-windows-msvc
      - name: Upload Desktop artifacts
        uses: actions/upload-artifact@v4
        with:
          name: LetRecovery-Desktop
          path: desktop/target/x86_64-pc-windows-msvc/release/LetRecovery.exe
          retention-days: 30
      - name: Commit and push Cargo.lock (and Cargo.toml if changed)
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add Cargo.toml Cargo.lock
          git diff --quiet && git diff --staged --quiet || git commit -m "Update Cargo.lock via build"
          git push
  build-pe:
    runs-on: windows-latest
    steps:
      - name: Checkout source code
        uses: actions/checkout@v6
        with:
          submodules: recursive
      - uses: wy414012/toolchain@master
        with:
          profile: minimal
          toolchain: "stable" # stable
          override: true
      - name: build pe
        working-directory: pe
        run: |
          cargo build --release --all-features --target=x86_64-pc-windows-msvc
      - name: Upload PE artifacts
        uses: actions/upload-artifact@v4
        with:
          name: LetRecovery-PE
          path: pe/target/x86_64-pc-windows-msvc/release/LetRecoveryPE.exe
          retention-days: 30
      - name: Commit and push Cargo.lock (and Cargo.toml if changed)
        run: |
          git config --global user.name 'github-actions[bot]'
          git config --global user.email 'github-actions[bot]@users.noreply.github.com'
          git add Cargo.toml Cargo.lock
          git diff --quiet && git diff --staged --quiet || git commit -m "Update Cargo.lock via build"
          git push
