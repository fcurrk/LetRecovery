# diskpart 脚本目录

把要在**装机前**自动执行的脚本放在本目录（程序目录 `\bin\diskpart\`）：

- `.cmd` / `.bat` 文件：通过 `cmd` 执行
- `.txt` 文件：通过 `diskpart /s` 执行
- 其它扩展名（如本说明 `.md`）会被忽略

在「系统安装」页勾选 **运行Diskpart脚本** 后，程序会在**格式化 / 释放镜像之前**、于 PE 中
按文件名顺序执行本目录下的所有脚本，可用于自定义分区等准备工作。

> ⚠️ diskpart 可直接清空磁盘/分区，请谨慎编写脚本，避免误删数据。
